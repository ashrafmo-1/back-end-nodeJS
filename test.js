// get single user
// app.get("/api/users/1", (req, res) => {
//   res.json(users.find((c) => c.id === 1))
// })




// handle requre by hand made
// if (!req.body.name) {
//   return r
// } else if (!req.body.email) {
//   return res.status(400).json({ error: "Missing email"});
// }